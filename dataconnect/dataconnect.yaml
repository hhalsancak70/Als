specVersion: "v1beta"
serviceId: "hobby"
location: "europe-west1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "hobby-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
